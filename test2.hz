fun fib(n) {
    if (n < 2) return n;
    return fib(n - 1) + fib(n - 2);
}

fun main() {
    var before = clock();
    println(fib(40));
    var after = clock();
    println(after - before);
}

main();